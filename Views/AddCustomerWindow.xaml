<Window x:Class="FFPOS.Views.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:controls="clr-namespace:FFPOS.Controls"
        Title="РќРѕРІС‹Р№ РєР»РёРµРЅС‚"
        Width="520"
        Height="430"
        MinWidth="480"
        MinHeight="390"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        FontFamily="Inter, Segoe UI"
        Loaded="WindowLoaded">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="8" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="28" ShadowDepth="4" Opacity="0.16" Color="#0B1220" />

        <Style x:Key="FlatButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="44" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#102033" />
            <Setter Property="BorderBrush" Value="#DDE5EE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFF7F6" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFB8B3" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFEDEA" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource FlatButtonStyle}">
            <Setter Property="Background" Value="#FF2B22" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style x:Key="InputStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Height" Value="44" />
            <Setter Property="Padding" Value="44,0,12,0" />
            <Setter Property="Background" Value="#FBFCFE" />
            <Setter Property="BorderBrush" Value="#DDE5EE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="RootBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="#1769FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="12" Background="White" BorderBrush="#DDE5EE" BorderThickness="1" CornerRadius="14" Effect="{StaticResource WindowShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="58" />
                <RowDefinition Height="*" />
                <RowDefinition Height="76" />
            </Grid.RowDefinitions>

            <Border BorderBrush="#E8EDF3" BorderThickness="0,0,0,1" Padding="18,0" MouseLeftButtonDown="HeaderMouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="AccountPlusOutline" Width="24" Height="24" Foreground="#111827" Margin="0,0,10,0" />
                        <TextBlock Text="РќРѕРІС‹Р№ РєР»РёРµРЅС‚" FontSize="21" FontWeight="Bold" Foreground="#111827" />
                    </StackPanel>
                    <controls:WindowControlButtons HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="24,20,24,10">
                <TextBlock Text="РќР°Р·РІР°РЅРёРµ РєР»РёРµРЅС‚Р°" FontSize="14" FontWeight="Bold" Foreground="#667085" Margin="0,0,0,6" />
                <Grid>
                    <TextBox x:Name="NameBox" Style="{StaticResource InputStyle}" />
                    <materialDesign:PackIcon Kind="AccountOutline" Width="20" Height="20" Foreground="#98A2B3" Margin="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" />
                </Grid>

                <TextBlock Text="РўРµР»РµС„РѕРЅ" FontSize="14" FontWeight="Bold" Foreground="#667085" Margin="0,14,0,6" />
                <Grid>
                    <TextBox x:Name="PhoneBox" Style="{StaticResource InputStyle}" />
                    <materialDesign:PackIcon Kind="PhoneOutline" Width="20" Height="20" Foreground="#98A2B3" Margin="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" />
                </Grid>

                <TextBlock Text="РђРґСЂРµСЃ" FontSize="14" FontWeight="Bold" Foreground="#667085" Margin="0,14,0,6" />
                <Grid>
                    <TextBox x:Name="AddressBox" Style="{StaticResource InputStyle}" />
                    <materialDesign:PackIcon Kind="MapMarkerOutline" Width="20" Height="20" Foreground="#98A2B3" Margin="14,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" />
                </Grid>

                <TextBlock x:Name="ErrorText" Foreground="#FF2B22" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,0" />
            </StackPanel>

            <Border Grid.Row="2" BorderBrush="#E8EDF3" BorderThickness="1,0,0,0" Padding="20,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Style="{StaticResource FlatButtonStyle}" Width="130" Margin="0,0,10,0" Content="РћС‚РјРµРЅР°" Click="CancelClicked" />
                        <Button Style="{StaticResource PrimaryButtonStyle}" Width="170" Content="РЎРѕС…СЂР°РЅРёС‚СЊ" Click="SaveClicked" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
