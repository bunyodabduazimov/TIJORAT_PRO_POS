<Window x:Class="FFPOS.Views.SyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:controls="clr-namespace:FFPOS.Controls"
        Title="Синхронизация"
        Width="580"
        Height="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Inter, Segoe UI">
    <Window.Resources>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="24" ShadowDepth="4" Opacity="0.16" Color="#0B1220" />

        <Style x:Key="RedButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="50" />
            <Setter Property="Background" Value="#F91F25" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E91920" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#D9151B" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProgressBarStyle" TargetType="{x:Type ProgressBar}">
            <Setter Property="Height" Value="10" />
            <Setter Property="Background" Value="#EEF2F7" />
            <Setter Property="Foreground" Value="#F91F25" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <Grid x:Name="PART_Track" ClipToBounds="True">
                                <Border x:Name="PART_Indicator"
                                        HorizontalAlignment="Left"
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="5" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White"
            BorderBrush="#D7DEE8"
            BorderThickness="1"
            CornerRadius="10"
            Effect="{StaticResource WindowShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="Transparent"
                    BorderBrush="#E8EDF3"
                    BorderThickness="0,0,0,1"
                    Padding="10,0"
                    PreviewMouseLeftButtonDown="HeaderMouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="35" Height="35" CornerRadius="21" Background="#FFF0EF">
                            <materialDesign:PackIcon Kind="Sync" Width="24" Height="24" Foreground="#F91F25" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="Синхронизация"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#111827"
                                   Margin="14,0,0,0"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <controls:WindowControlButtons ShowMaximize="False" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20,10,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="22" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="StatusTitle"
                           Text="Готово к синхронизации"
                           FontSize="19"
                           FontWeight="Bold"
                           Foreground="#111827" />

                <StackPanel Grid.Row="2">
                    <TextBlock x:Name="StatusMessage"
                               Text="Нажмите кнопку, чтобы получить данные с сервера."
                               FontSize="16"
                               FontWeight="Medium"
                               Foreground="#667085"
                               TextWrapping="Wrap" />
                    <ProgressBar x:Name="SyncProgressBar"
                                 Style="{StaticResource ProgressBarStyle}"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="0"
                                 Margin="0,14,0,0" />
                </StackPanel>

                <Border Grid.Row="4"
                        Background="#F8FAFC"
                        BorderBrush="#E1E7EF"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="14">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="10" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Button x:Name="CopyErrorButton"
                                Grid.Row="0"
                                Visibility="Collapsed"
                                HorizontalAlignment="Right"
                                Height="32"
                                Padding="12,0"
                                Background="White"
                                BorderBrush="#DDE3EB"
                                BorderThickness="1"
                                Foreground="#344054"
                                Cursor="Hand"
                                FocusVisualStyle="{x:Null}"
                                Click="CopyErrorClicked">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16" Margin="0,0,8,0" />
                                <TextBlock Text="Скопировать ошибку" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="LogText"
                                       Text="Ожидание..."
                                       FontSize="15"
                                       FontWeight="Medium"
                                       Foreground="#344054"
                                       TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Grid>
                </Border>

                <Button x:Name="SyncButton"
                        Grid.Row="6"
                        Style="{StaticResource RedButtonStyle}"
                        Click="SyncClicked">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Sync" Width="25" Height="25" Margin="0,0,12,0" />
                        <TextBlock Text="Синхронизация" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
