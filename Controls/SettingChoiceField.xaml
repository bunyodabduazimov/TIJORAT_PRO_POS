<UserControl x:Class="FFPOS.Controls.SettingChoiceField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root"
             FontFamily="Inter, Segoe UI">
    <UserControl.Resources>
        <Style x:Key="ChoiceButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Height" Value="34" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="6" />
            <RowDefinition Height="42" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Header, ElementName=Root}"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="#667085" />

        <Border Grid.Row="2"
                Background="#FBFCFE"
                BorderBrush="#DDE3EB"
                BorderThickness="1"
                CornerRadius="8"
                Padding="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button x:Name="LeftButton"
                        Style="{StaticResource ChoiceButtonStyle}"
                        Click="LeftClicked">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon x:Name="LeftIcon" Kind="Target" Width="17" Height="17" Margin="0,0,8,0" />
                        <TextBlock x:Name="LeftLabel" Text="{Binding LeftText, ElementName=Root}" FontSize="14" FontWeight="Bold" />
                    </StackPanel>
                </Button>

                <Button x:Name="RightButton"
                        Grid.Column="2"
                        Style="{StaticResource ChoiceButtonStyle}"
                        Click="RightClicked">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon x:Name="RightIcon" Kind="PackageVariantClosed" Width="17" Height="17" Margin="0,0,8,0" />
                        <TextBlock x:Name="RightLabel" Text="{Binding RightText, ElementName=Root}" FontSize="14" FontWeight="Bold" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
